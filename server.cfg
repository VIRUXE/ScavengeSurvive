echo open.mp is configured via config.json. This server.cfg only supplies
echo legacy options that SA-MP plugins read from server.cfg directly.
echo long_call_time 0 disables crashdetect's long-callback detection, whose
echo 5000us default false-positives during the long world load.
long_call_time 0
